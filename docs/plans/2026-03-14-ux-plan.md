# 用户体验打磨实施计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development to implement this plan.

**Goal:** 优化动画性能、添加骨架屏和完善暗色模式

**Tech Stack:** Flutter Animation, Shimmer

---

## 任务 1: 添加骨架屏加载

- [ ] **Step 1: 添加 shimmer 依赖**

```yaml
dependencies:
  shimmer: ^3.0.0
```

- [ ] **Step 2: 创建骨架屏组件 `lib/presentation/widgets/shimmer_loading.dart`**

```dart
import 'package:flutter/material.dart';
import 'package:shimmer/shimmer.dart';

class ShimmerLoading extends StatelessWidget {
  final Widget child;
  final bool isLoading;

  const ShimmerLoading({
    super.key,
    required this.child,
    this.isLoading = true,
  });

  @override
  Widget build(BuildContext context) {
    if (!isLoading) return child;

    return Shimmer.fromColors(
      baseColor: Colors.grey.shade300,
      highlightColor: Colors.grey.shade100,
      child: child,
    );
  }
}
```

- [ ] **Step 3: 提交**

```bash
git add pubspec.yaml lib/presentation/widgets/shimmer_loading.dart
git commit -m "ui: add shimmer loading skeleton"
```

---

## 任务 2: 优化暗色模式

- [ ] **Step 1: 更新主题配置 `lib/core/theme/app_theme.dart`**

增强暗色模式配置:

```dart
static ThemeData get darkTheme {
  return ThemeData(
    useMaterial3: true,
    brightness: Brightness.dark,
    // 增强暗色模式对比度
    colorScheme: ColorScheme.dark(
      surface: const Color(0xFF1E1E1E),
      onSurface: Colors.white,
    ),
  );
}
```

- [ ] **Step 2: 提交**

```bash
git add lib/core/theme/app_theme.dart
git commit -m "ui: improve dark theme contrast"
```

---

## 任务 3: 添加加载动画优化

- [ ] **Step 1: 创建自定义动画组件 `lib/presentation/widgets/loading_indicator.dart`**

```dart
class LoadingIndicator extends StatefulWidget {
  final double size;
  final Color? color;

  const LoadingIndicator({super.key, this.size = 24, this.color});

  @override
  State<LoadingIndicator> createState() => _LoadingIndicatorState();
}

class _LoadingIndicatorState extends State<LoadingIndicator>
    with SingleTickerProviderStateMixin {
  late AnimationController _controller;

  @override
  void initState() {
    super.initState();
    _controller = AnimationController(
      duration: const Duration(milliseconds: 1500),
      vsync: this,
    )..repeat();
  }

  @override
  void dispose() {
    _controller.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: widget.size,
      height: widget.size,
      child: CircularProgressIndicator(
        strokeWidth: 2,
        valueColor: AlwaysStoppedAnimation<Color>(
          widget.color ?? Theme.of(context).colorScheme.primary,
        ),
      ),
    );
  }
}
```

- [ ] **Step 2: 提交**

```bash
git add lib/presentation/widgets/loading_indicator.dart
git commit -m "ui: add optimized loading indicator"
```

---

## 总结: 3 个提交
